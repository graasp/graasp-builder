
// todo: use graasp sdk
export type Category = {
  id: string;
  name: string;
};

export type Invitation = {
  id: string,
  email: string,
  permission: string,
  createdAt: string,
  updatedAt: string,
}
