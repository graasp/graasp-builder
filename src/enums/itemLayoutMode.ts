enum ItemLayoutMode {
  Grid = 'grid',
  List = 'list',
  // todo: enable when adding the map
  // Map = 'map',
}

export const DEFAULT_ITEM_LAYOUT_MODE = ItemLayoutMode.List;

export default ItemLayoutMode;
